@model List<OrcamentoModel>

<div class="card-body">
    <div class="table-responsive">
        <table class="table table-striped">
            <thead>
                <tr>
                    <th scope="col">Código</th>
                    <th scope="col">Orbservação</th>
                    <th scope="col">Descrição</th>
                    <th scope="col">Entrega</th>
                    <th scope="col">Data de inclusão</th>
                </tr>
            </thead>
            <tbody>
                @if (Model != null && Model.Any())
                {
                    foreach (var orcamento in Model)
                    {
                        <tr>
                            <th scope="row">@orcamento.ID</th>
                            <td>@orcamento.Observacoes</td>
                            <td>@orcamento.Descricao</td>
                            <td>@orcamento.TipoEntrega</td>
                            <td>@orcamento.DataInclusao</td>
                        </tr>
                    }
                }
                else
                {
                    <tr>Nenhum orçamento encontrado</tr>
                }
            </tbody>
        </table>
    </div>
</div>
